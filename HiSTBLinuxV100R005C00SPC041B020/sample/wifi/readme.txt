1. For STA
  1)copy sample_sta to /sbin directory of device, then run 'chmod a+x sample_sta'.
  2)run sample_sta

2. For SoftAP
  1)copy sample_ap to /sbin directory of device, then run 'chmod a+x sample_ap'.
  2)copy udhcpd.conf to /etc directory.
  3)run sample_ap
 

